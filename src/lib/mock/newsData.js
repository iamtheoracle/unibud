/**
 * Neutralized — no mock news data.
 * Real news comes from FootballNews entity. Empty until real data exists.
 */
export const NEWS_CATEGORIES = [];
export const NEWS_ITEMS = [];
export function newsByCategory() { return []; }
export function categoryByKey() { return null; }