import type { ICapability } from '../types/index.ts';

export const basicCapability: ICapability = {
  name: 'basic-capability',
  version: '1.0.0',
  provider: 'basic-module',
  description: 'Minimal capability descriptor example',
};
