/**
 * MainTabBar — backward-compat shim.
 * The canonical navigation bar is now PrimaryNavBar.
 * This file re-exports PrimaryNavBar so existing imports don't break.
 */
export { default } from "@/components/layout/PrimaryNavBar";
