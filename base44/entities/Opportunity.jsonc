{
  "name": "Opportunity",
  "type": "object",
  "properties": {
    "title": {
      "type": "string"
    },
    "organization": {
      "type": "string"
    },
    "type": {
      "type": "string",
      "enum": [
        "scholarship",
        "internship",
        "job",
        "graduate_job",
        "campus_job",
        "competition",
        "hackathon",
        "exchange",
        "research",
        "grant",
        "fellowship",
        "volunteering",
        "mentorship"
      ],
      "default": "internship"
    },
    "description": {
      "type": "string"
    },
    "deadline": {
      "type": "string",
      "format": "date"
    },
    "location": {
      "type": "string"
    },
    "amount": {
      "type": "string"
    },
    "eligibility": {
      "type": "string"
    },
    "link": {
      "type": "string"
    },
    "is_saved": {
      "type": "boolean",
      "default": false
    },
    "favorited_by": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "User IDs who bookmarked this opportunity"
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "institution_id": {
      "type": "string",
      "description": "Tenant scope \u2014 Institution id"
    }
  },
  "required": [
    "title",
    "type"
  ],
  "rls": {
    "read": {
      "$or": [
        {
          "data.institution_id": "{{user.data.institution_id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "create": {
      "data.institution_id": "{{user.data.institution_id}}"
    },
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}